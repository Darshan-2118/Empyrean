// useAuth hook — thin re-export so hooks/ is the canonical import location.
// All components should import from here: import { useAuth } from '../hooks/useAuth'
export { useAuth } from '../services/useAuth';
